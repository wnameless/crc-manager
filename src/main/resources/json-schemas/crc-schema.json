{
  "definitions": {
    "Drug": {
      "type": "string",
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "antacid"
          ],
          "title": "胃藥"
        },
        {
          "type": "string",
          "enum": [
            "steroid"
          ],
          "title": "類固醇"
        },
        {
          "type": "string",
          "enum": [
            "antibiotic"
          ],
          "title": "抗生素"
        }
      ]
    }
  },
  "title": "禁忌用藥專區",
  "type": "object",
  "properties": {
    "contraindicationFlg": {
      "type": "string",
      "title": "1. 本案是否有禁忌用藥?",
      "enum": [
        "是",
        "否"
      ]
    },
    "drugs": {
      "type": "array",
      "title": "若是，請填入禁忌用藥名稱",
      "items": {
        "$ref": "#/definitions/Drug"
      }
    }
  }
}